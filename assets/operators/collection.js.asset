let numbers = [1,2,3,4,5,6,7,8]

numbers
    .forEach(x => context.log(x))
###
let numbers = [1,2,3,4,5,6,7,8]

numbers
    .map(x => x * 2)
    .forEach(x => context.log(x))
###
let numbers = [1,2,3,4,5,6,7,8]

numbers
    .map(x => x * 2)
    .filter(x => x !== 8)
    .forEach(x => context.log(x))